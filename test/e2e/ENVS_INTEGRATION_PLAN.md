# Plan: Build and integrate the remaining E2E stacks (envs) so all legacy aggkit-CI test stacks become runnable on the new Go/docker-compose stack

## Task Details

- **Task summary:** Create the missing test **environments** on `0xPolygon/kurtosis-cdk` (as kurtosis snapshots) and integrate them into the new Go/docker-compose e2e stack in `agglayer/aggkit` (`test/e2e/envs/<name>/` + loader + sanity checks + new-stack CI), so that *every* network topology currently exercised by the legacy aggkit CI (`.github/workflows/test-e2e.yml`) can be loaded and run on the new stack. The existing `MIGRATION_PLAN.md` only covers the single `op-pp` env; this plan adds the rest of the stacks. **This plan is scoped to env creation + integration only — it does NOT port/migrate any test logic.**
- **Background/context:** The new stack (`aggkit/test/e2e`, Go + `docker compose`) loads a frozen network via `test/e2e/envs/loader.go`, which reads a kurtosis **snapshot** (`docker-compose.yml` + `summary.json` + `config/`) produced by `0xPolygon/kurtosis-cdk`'s snapshot tool (`snapshot/snapshot.sh`, see `docs/docs/advanced/snapshot.md`). Today only one env exists: `op-pp` (single OP-Stack L2 in PessimisticProof mode, native ETH). The legacy aggkit CI (`test-e2e.yml`) additionally runs four other stacks via the reusable `agglayer/e2e` kurtosis workflows:
  1. `test-single-l2-network-op-succinct` — single OP-Stack L2 in **FEP / op-succinct** mode (mock prover; `consensus_contract_type: fep`).
  2. `test-single-l2-network-op-succinct-aggoracle-committee` — FEP + **aggoracle committee** (`use_agg_oracle_committee: true`, quorum 2 of 3).
  3. `test-multi-l2-networks-2-chains-op-pessimistic` — **2 OP-PP L2s** under one L1/agglayer (chains `001` + `002`).
  4. `test-multi-l2-networks-3-chains-cdk-erigon-pessimistic` — **3 cdk-erigon L2s**, one of which has a **custom gas token** (`gas_token_enabled: true`).
  These four stacks are the source of the test files that `MIGRATION_PLAN.md` lists as out-of-scope (`bridge-e2e-custom-gas.bats`, `bridge-e2e-2-chains.bats`, `bridge-e2e-3-chains.bats`, `bridge-e2e-aggoracle-committee.bats`, `tests/op/optimistic-mode.bats`, FEP cases in `latest-n-injected-ger.bats`). Each is out-of-scope **only because its env does not exist on the new stack** — this plan removes that blocker.
- **Expected outcome:** Four new envs exist as committed kurtosis snapshots under `aggkit/test/e2e/envs/` — `op-fep`, `op-fep-committee`, `op-pp-2chains`, `cdk-erigon-3chains` — each loadable by `LoadEnv`, passing its sanity checks (`envs/checks.go`) and a minimal post-load health probe, and brought up green in the new-stack CI (`test-go-e2e.yml`). The kurtosis-cdk side ships on a dedicated branch (env config presets + any snapshot-tool changes needed to capture multi-L2 / FEP / cdk-erigon / custom-gas topologies). The aggkit loader/checks/CI are generalized from single-network/native-ETH to N-network and non-native-gas. **No legacy test is ported in this plan** — that is follow-on work (extensions of `MIGRATION_PLAN.md`).
- **Deliverable path:** `/home/aigent/repos/agglayer/aggkit/test/e2e/ENVS_INTEGRATION_PLAN.md` (this file).
- **Workspaces/repositories involved:**
  - **kurtosis-cdk:** `/home/aigent/repos/0xPolygon/kurtosis-cdk` (currently on `main`). All env config presets + snapshot-tool work go on a **new branch** (suggested: `feat/aggkit-e2e-envs`). **All steps writing here must be serialized** (single workspace). Produces a commit SHA that aggkit's snapshot-generation steps and `test-e2e.yml`'s `KURTOSIS_CDK_COMMIT` may later pin.
  - **aggkit:** `/home/aigent/repos/agglayer/aggkit` is **occupied** — another agent is actively working there on branch `feat/migrate-e2e` with **uncommitted changes** (`test/e2e/README.md`, `helpers_test.go`, `MIGRATION_PLAN.md`). **Do NOT work in that checkout.** Create a **git worktree** for this plan (suggested path `/home/aigent/repos/agglayer/aggkit-envs`, new branch `feat/e2e-envs-integration` based off `origin/<default>` or off `feat/migrate-e2e` — decide in P1). **All steps writing to the worktree must be serialized** (single workspace). Coordinate the eventual merge order with the other agent (P12).
- **Resources to inspect:**
  - kurtosis-cdk: `snapshot/snapshot.sh`, `snapshot/verify.sh`, `docs/docs/advanced/snapshot.md`, `main.star`, `.github/tests/op-succinct/{mock-prover,real-prover}.yml`, `.github/tests/cdk-erigon/*.yml`, `.github/tests/other/gas-token/*.yml`, the existing `snapshots/` outputs as reference for snapshot directory layout.
  - aggkit: `test/e2e/envs/loader.go` (hardcodes L2 network `"001"`, native-ETH MintableERC20 deploy, single aggkit service `aggkit-001`), `test/e2e/envs/checks.go`, `test/e2e/envs/op-pp/{docker-compose.yml,summary.json,config/}`, `test/e2e/envs/README.md`, `test/e2e/README.md`, `test/e2e/testmain_test.go`, `.github/workflows/test-go-e2e.yml` (hardcodes `cd test/e2e/envs/op-pp` and a fixed `docker compose pull` image list), `Makefile` (`test-e2e`).
  - Legacy CI source-of-truth for each stack's exact kurtosis args: aggkit `.github/test_e2e_op_succinct_args_base.json`, `.github/test_e2e_single_chain_op_succinct_args.json`, `.github/test_e2e_single_chain_op_succinct_aggoracle_committee_args.json`, `.github/test_e2e_op_args_chain_1.json`, `.github/test_e2e_op_args_chain_2.json`, `.github/test_e2e_cdk_erigon_args_base.json`, `.github/test_e2e_cdk_erigon_custom_gas_token.json`, `.github/test_e2e_cdk_erigon_multi_chains_args_{2,3}.json`, `.github/workflows/test-e2e.yml`.
- **Validation requirements:**
  - For each new env: `kurtosis run` of its config on the kurtosis-cdk branch comes up healthy; `snapshot/snapshot.sh` produces a snapshot whose `verify.sh` passes; the snapshot copied into aggkit boots via `docker compose up -d` from `test/e2e/envs/<name>/`; `LoadEnv(ctx, "<name>")` succeeds; `envs/checks.go` sanity checks pass; a minimal post-load probe (one bridge or a health check appropriate to the topology) succeeds — **all without running any migrated bats/Go test logic**.
  - aggkit worktree stays green on `make build` and `make lint` after every loader/checks/CI change.
  - `test-go-e2e.yml` (extended to a matrix over all envs) brings up each new env and runs its (currently empty / smoke-only) test selection green within timeout.
  - Snapshot prerequisite honored: **no agglayer settlement / bridge activity before snapshotting** (per `snapshot.md`), else restored snapshots are inconsistent.
- **Non-goals:**
  - **Do NOT port or write any test** (no bridge/GER/committee/custom-gas/multi-chain test logic). That is follow-on work tracked by `MIGRATION_PLAN.md` extensions.
  - Do not modify the legacy `test-e2e.yml` job matrix to *remove* coverage, and do not touch the `agglayer/e2e` reusable workflows — retiring legacy jobs happens only after the corresponding tests are migrated (out of scope here).
  - Do not change production aggkit component code beyond what loading/validating an env requires.
  - Do not work inside the occupied `/home/aigent/repos/agglayer/aggkit` checkout; use the worktree.
  - Do not merge the kurtosis-cdk or aggkit branches without the explicit coordination/handoff in P12.
- **Special constraints:**
  - **Worktree isolation:** another agent owns `feat/migrate-e2e` in the main aggkit checkout. This plan's aggkit writes happen only in the worktree. Rebase/merge coordination is a dedicated step (P12).
  - **Single workspace serialization:** kurtosis-cdk and the aggkit worktree are each one working tree; steps that write to the same tree must not run concurrently. Reads may overlap. The dependency graph below enforces this.
  - **Loader is single-network today:** `summaryJSON` parses `l2_networks` as a map but `LoadEnv` only reads `"001"`, deploys a native-ETH `MintableERC20`, and assumes one `aggkit-001` service. Multi-chain and custom-gas envs require generalizing this before they can be integrated (P5).
  - **Snapshot tool scope:** `snapshot.md` documents L1 geth+beacon+validator capture and a single-`docker-compose.yml` output; multi-L2, op-succinct (FEP prover/proposer), and cdk-erigon datadir topologies may need snapshot-tool extensions and a `summary.json` that lists multiple `l2_networks` (P3).
  - **Prefer config reuse:** derive each env's kurtosis args from the existing aggkit `.github/test_e2e_*.json` files and kurtosis-cdk `.github/tests/*` presets rather than inventing new topology.
  - **Determinism:** record the exact kurtosis-cdk commit + the kurtosis args used to generate each snapshot in that env's `README`/metadata (mirror the existing `envs/README.md` `op-pp` entry).
- **Known risks or edge cases:**
  - **op-succinct/FEP** requires a prover + proposer/settlement path; the mock prover (`op_succinct_mock: true`) is the CI default. Snapshotting an FEP enclave must avoid pre-snapshot settlement (snapshot prerequisite) yet the env must still be able to settle *after* boot — verify FEP settlement works post-restore.
  - **Multi-chain** snapshots must capture multiple op-geth/op-node/aggkit instances and emit a `summary.json` with multiple `l2_networks`; ports must not collide in the generated compose; the loader must expose all L2s (not just `001`).
  - **cdk-erigon** uses a different sequencer (erigon, not op-geth) with different datadir layout and service names — both the snapshot tool's datadir capture and the loader's RPC/contract wiring differ from the op-stack path.
  - **Custom gas token** means L1↔L2 value transfer is via the gas-token ERC20, not native ETH; the loader's native-ETH assumptions (and the auto-deployed `MintableERC20`) need conditioning per env.
  - **Aggoracle committee** needs extra validator/committee services + keystores in the snapshot (the same gap `MIGRATION_PLAN.md` P10 flagged for op-pp); ensure the committee containers are captured and start healthy from the snapshot.
  - **CI image pull/runtime:** `test-go-e2e.yml`'s `pull-docker-images` job hardcodes the `op-pp` service list; each new env adds new images (op-succinct prover, cdk-erigon, committee). Multi-chain envs are heavier (more containers) and may exceed the current 45m job timeout and host resources.
  - **Snapshot drift:** kurtosis-cdk `main` evolves; pin the generating commit per env so snapshots are reproducible (the legacy `test-e2e.yml` pins `KURTOSIS_CDK_COMMIT`).
  - **Env count vs. coverage (resolved):** `op-fep-committee` is a **separate env** (confirmed), and `cdk-erigon-3chains` provides the custom-gas coverage so **no separate single custom-gas env** is built. The target env set is the four in the table — no further env-count decisions remain.

### Target env set (legacy CI stack → new env)

| Legacy `test-e2e.yml` job | Topology | New env dir | Source kurtosis args (aggkit `.github/`) |
|---|---|---|---|
| `test-single-l2-network-op-succinct` | 1× OP-Stack L2, FEP/op-succinct (mock prover) | `op-fep` | `test_e2e_op_succinct_args_base.json` + `test_e2e_single_chain_op_succinct_args.json` |
| `test-single-l2-network-op-succinct-aggoracle-committee` | FEP + aggoracle committee (2/3) | `op-fep-committee` *(separate env — confirmed)* | `..._op_succinct_args_base.json` + `..._single_chain_op_succinct_aggoracle_committee_args.json` |
| `test-multi-l2-networks-2-chains-op-pessimistic` | 2× OP-PP L2 (`001`+`002`), shared L1/agglayer | `op-pp-2chains` | `test_e2e_op_args_chain_1.json` + `test_e2e_op_args_chain_2.json` |
| `test-multi-l2-networks-3-chains-cdk-erigon-pessimistic` | 3× cdk-erigon L2, one custom-gas | `cdk-erigon-3chains` | `test_e2e_cdk_erigon_args_base.json` + `..._multi_chains_args_{2,3}.json` + `..._custom_gas_token.json` |

> Decisions confirmed: `op-fep-committee` is a **separate env** (not a toggle on `op-fep`). The standalone `bridge-e2e-custom-gas.bats` coverage is provided by the custom-gas chain inside `cdk-erigon-3chains` — **no dedicated single custom-gas env**.

## Execution Plan

### P1. Discovery, decisions, and env spec lock-in

- Status: completed
- Execution notes (P1 outcome — see `ENVS_INTEGRATION_PLAN/P1_LOG.md`): Spec produced at `/tmp/follow-plan/run-20260529-150038/P1/execution_deliverable.md`. Key locked decisions for downstream steps: (1) **Worktree base = `origin/develop`** (not the dirty, agent-owned `feat/migrate-e2e`) — P2 fallback if the op-pp loader baseline turns out to live only on `feat/migrate-e2e`. (2) **`cdk-erigon-3chains` applies `custom_gas_token.json` to chains 001 AND 002** (003 native), not just one chain as the prose one-liner implies — affects P4 preset + P10. (3) `test_e2e_op_succinct_args_base.json` enables `bridge_spammer` via `additional_services: ["bridge_spammer"]` (array entry) — must be dropped for snapshot-clean FEP presets (P4/P7/P8). (4) Snapshot `summary.json` has an `op-reth`-emitted vs `op-geth`-consumed service-key mismatch — address in P3. (5) Loader hardcodes confirmed: `L2Networks["001"]` (loader.go:240), unconditional `DeployMintableerc20` (~loader.go:312), `aggkitServiceName = "aggkit-001"` (loader.go:527) — P5 targets.
- Goal: Produce a short written spec (committed as a scratch note in the aggkit worktree once it exists in P2, or inline in this section's PR description) that fixes: the exact env set + names, whether committee is its own env vs. a toggle, whether a standalone custom-gas env is needed, the worktree base branch, and the concrete snapshot-tool / loader gaps each env will hit.
- Context pack: this file's *Target env set* table; aggkit `.github/test_e2e_*.json` (all listed above) and `.github/workflows/test-e2e.yml`; kurtosis-cdk `main.star`, `docs/docs/advanced/snapshot.md`, `snapshot/snapshot.sh`, `.github/tests/op-succinct/*`, `.github/tests/cdk-erigon/*`, `.github/tests/other/gas-token/*`; aggkit `test/e2e/envs/loader.go` + `checks.go` + `op-pp/summary.json` (note single-`001` parsing, native-ETH MintableERC20 deploy, `aggkit-001` hardcode); aggkit git state (`feat/migrate-e2e`, dirty) to choose the worktree base.
- Actions:
  - Confirm final dir names for the 4-env set and record the kurtosis args composition for each (which JSON presets merge to form it). *(Env set is locked: `op-fep`, `op-fep-committee` (separate env), `op-pp-2chains`, `cdk-erigon-3chains`; no standalone custom-gas env.)*
  - Decide the worktree base branch (clean default branch vs. `feat/migrate-e2e`) and the merge/coordination story with the active agent (feeds P12).
  - Enumerate, per env, the expected snapshot-tool gaps (multi-L2 capture, FEP prover/proposer services, cdk-erigon datadirs, committee keystores) and loader gaps (N-network exposure, non-native gas, cdk-erigon RPC/contract wiring).
- Acceptance criteria: a written spec exists listing final env names, per-env kurtosis-args composition, the worktree base branch, and a gap list for snapshot-tool (P3) and loader (P5). No code changed. *(Env-count decisions are already settled — see locked set above.)*
- Non-goals: Writing configs, generating snapshots, or editing any repo other than recording the decision note.
- Dependencies: None

### P2. Create the kurtosis-cdk branch and aggkit worktree (workspace setup)

- Status: completed
- Execution notes (P2 outcome — see `ENVS_INTEGRATION_PLAN/P2_LOG.md`): **kurtosis-cdk branch `feat/aggkit-e2e-envs`** checked out off `main` (P3/P4/P7–P10 write here). **aggkit worktree at `/home/aigent/repos/agglayer/aggkit-envs` on branch `feat/e2e-envs-integration`, based on `origin/develop`** (P5/P6/P7–P10/P12 write here). `make build` green; worktree clean. **P1 fallback NOT triggered** — `loader.go`, `checks.go`, and `op-pp/summary.json` all exist on `origin/develop`, so the worktree is independent of the other agent's `feat/migrate-e2e` → **P12 merge coordination is simplified** (no dependency on the WIP branch). A stale worktree entry `/tmp/aggkit-pr1608-check` was pruned.
- Goal: Establish the two isolated working trees this plan writes to.
- Context pack: kurtosis-cdk `git` state (`main`); aggkit `git worktree list` (main checkout on dirty `feat/migrate-e2e`; an unrelated prunable worktree exists under `/tmp`); P1's worktree-base decision.
- Actions:
  - In kurtosis-cdk, create and check out a new branch (suggested `feat/aggkit-e2e-envs`) off `main`.
  - For aggkit, create a git worktree at the suggested path `/home/aigent/repos/agglayer/aggkit-envs` on a new branch (suggested `feat/e2e-envs-integration`) from the **P1-chosen base = `origin/develop`** — **without disturbing the occupied main checkout**. Verify `git -C <worktree> status` is clean and `make build` works there. **P1 fallback note:** if the `test/e2e/envs/loader.go` + `op-pp` baseline this plan generalizes does NOT exist on `origin/develop` (i.e. it lives only on the other agent's `feat/migrate-e2e`), branch off `feat/migrate-e2e` instead and record the change for the P12 coordination story.
- Acceptance criteria: kurtosis-cdk branch checked out; aggkit worktree exists at the chosen path on its own branch, builds, and is independent of the `feat/migrate-e2e` working tree.
- Non-goals: Any config/loader/snapshot change yet.
- Dependencies: P1

### P3. Extend the kurtosis-cdk snapshot tool for the new topologies

- Status: completed
- Execution notes (P3 outcome — THUMBS_UP attempt 2, 1 change request; see `ENVS_INTEGRATION_PLAN/P3_LOG.md`): On kurtosis-cdk branch `feat/aggkit-e2e-envs`. Iter1 `0fe7bf4b` = op-reth→op-geth emitter reconciliation (loader-facing keys/hostnames now `op-geth`; internal kurtosis discovery names left `op-reth`) + multi-L2 `summary.json` emission. Iter2 `5f06bd83` = per-topology **capture** logic added across 5 snapshot scripts, keyed off a new per-network **`chain_type`** field (default `op-stack`, backward-compatible). **For P4/P7–P10:** emitted summary service keys per topology — `op-geth`/`op-node`/`aggkit` (op-stack), `op-succinct-proposer` (FEP, emits `settled:false` to honor no-pre-snapshot-settlement), `cdk-erigon` (erigon EL, surfaces `contracts.gas_token` + erigon chain_id; tars `/home/erigon/data/dynamic-*-sequencer` + `/etc/cdk-erigon`), `cdk-data-availability` (committee/DAC, captures `dac.keystore` + committee-member account). **Infra-excused follow-up:** the LIVE `snapshot.sh`+`verify.sh` run was NOT executed (empty kurtosis enclave) — it must be exercised per-env in P7–P10 using their P4 args.
- Goal: Ensure `snapshot/snapshot.sh` (+ helpers) can capture and emit a correct snapshot (`docker-compose.yml`, `summary.json` with **all** `l2_networks`, `config/`) for: FEP/op-succinct single-chain, multi-L2 (≥2 op-stack), and cdk-erigon multi-chain (incl. a custom-gas chain), with committee services where present.
- Context pack: kurtosis-cdk `snapshot/snapshot.sh`, `snapshot/verify.sh`, `snapshot/snapshots/*/SNAPSHOT_METADATA.md`, `docs/docs/advanced/snapshot.md` (L1 capture flow, agglayer-settlement prerequisite, summary.json schema), `main.star`; aggkit `test/e2e/envs/op-pp/summary.json` (the consumed schema — `networks.l2_networks.<id>.{contracts,services,accounts}`) and `loader.go` `summaryJSON` struct (the fields the loader actually reads).
- Actions:
  - Audit the snapshot tool against each topology; implement the minimal extensions so the emitted `summary.json` includes every L2 network (not just `001`) with correct per-network `services` (op-geth/op-node/aggkit or erigon equivalents), `contracts`, and `accounts`, and so multi-service datadirs (op-succinct prover/proposer, cdk-erigon, committee) are captured and restartable.
  - Preserve the "no settlement before snapshot" guarantee for FEP (the prover must be wired but not have settled) and verify post-restore settlement still works.
  - **P1 finding:** resolve the `summary.json` service-key mismatch where the snapshot tool emits an `op-reth` key but `loader.go`/`op-pp` consume `op-geth` — reconcile so the emitted summary matches what the loader (P5) reads.
  - Keep changes backward-compatible with the existing `op-pp` snapshot flow.
- Acceptance criteria: on the kurtosis-cdk branch, `snapshot.sh` run against a representative enclave of each topology produces a snapshot that `verify.sh` accepts, and whose `summary.json` lists all L2 networks with the fields `loader.go` requires; the existing `op-pp` snapshot path is unaffected.
- Non-goals: Defining the per-env kurtosis args (P7–P10); any aggkit change.
- Dependencies: P2

### P4. Add kurtosis env-config presets on the kurtosis-cdk branch

- Status: completed
- Execution notes (P4 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P4_LOG.md`): 4 presets + README under kurtosis-cdk `.github/tests/aggkit-e2e-envs/` on branch `feat/aggkit-e2e-envs`, commit `bd3308c9`: `op-fep.yml`, `op-fep-committee.yml`, `op-pp-2chains.yml`, `cdk-erigon-3chains.yml` — each byte-faithful to the aggkit CI deep-merge (jq zero-diff except intended deviations). **For P7–P10:** `main.star` deploys ONE L2 per `kurtosis run`, so the two multi-chain envs are **multi-document YAML** (one doc/chain, applied sequentially into a shared enclave per legacy `aggkit-e2e-multi-chains.yml`; `op-pp-2chains` doc2 references doc1's `el_cl_genesis_data` — expected cross-doc shared-enclave dep). `bridge_spammer` dropped from both FEP envs; custom gas on cdk-erigon 001+002 (003 native); committee quorum 2/3. **Infra-excused follow-up:** the live healthy-enclave bring-up was deferred (heavy images) — P7–P10 run it per env with the documented `kurtosis run --args-file` commands.
- Goal: Commit reproducible kurtosis config presets (args files) for each target env on the kurtosis-cdk branch, derived from the aggkit `.github/test_e2e_*.json` compositions identified in P1, so snapshots are regenerable from version-controlled inputs.
- Context pack: aggkit `.github/test_e2e_*.json` set (FEP, committee, op chains 1/2, cdk-erigon base + multi + custom-gas); kurtosis-cdk `.github/tests/op-succinct/*`, `.github/tests/cdk-erigon/*`, `.github/tests/other/gas-token/*`, `main.star` arg surface; the snapshot prerequisite (exclude `bridge_spammer`/settlement-inducing `additional_services` so the enclave is snapshot-clean — **P1 confirmed** `test_e2e_op_succinct_args_base.json` sets `additional_services: ["bridge_spammer"]` (array entry, not a boolean), feeding BOTH FEP envs; must be dropped for snapshotting). **P1 finding:** the `cdk-erigon-3chains` preset must apply `custom_gas_token.json` to chains **001 AND 002** (003 native) to mirror the legacy CI — not a single custom-gas chain.
- Actions: Add one consolidated kurtosis args file per env under a clear location on the kurtosis-cdk branch (e.g. `.github/tests/aggkit-e2e-envs/{op-fep,op-fep-committee,op-pp-2chains,cdk-erigon-3chains}.yml`), each excluding pre-snapshot bridge/settlement activity; document each file's provenance (which aggkit JSONs it mirrors).
- Acceptance criteria: each preset `kurtosis run`s to a healthy enclave of the intended topology on the branch; presets are snapshot-clean (no pre-snapshot settlement/spammer); provenance documented.
- Non-goals: Generating/committing snapshots (done per-env in P7–P10); aggkit changes.
- Dependencies: P3

### P5. Generalize the aggkit env loader for N-network, FEP, custom-gas, and cdk-erigon

- Status: completed
- Execution notes (P5 outcome — see `ENVS_INTEGRATION_PLAN/P5_LOG.md`): `test/e2e/envs/loader.go` generalized (only that file changed, +435/-197) on worktree branch `feat/e2e-envs-integration` @ SHA `3c22e5f3`. **New loader API for P6/P7–P10 to use:** `Env.L2s []L2Config` (all networks); backward-compatible `Env.L2` accessor → primary (lowest-key) network; helpers `Env.PrimaryL2` and `Env.L2ByNetworkID(id)`; per-network capability flag `caps.NativeGas` gating the `MintableERC20` auto-deploy (ON for op-pp, OFF for cdk-erigon); parameterized aggkit service name(s). Four `ENVName` constants added: `op-fep`, `op-fep-committee`, `op-pp-2chains`, `cdk-erigon-3chains`. `make build` green; changed package lint-clean (repo-wide `make lint` fails only on 13 pre-existing unrelated findings; the gopls `stringsseq` hints are not in the configured linter). Docker runtime op-pp load not run (no infra) — compile + non-docker tests + `go vet` used. **P12 note:** loader reads L2 RPC from `op-geth` summary key; P3 reconciled the emitter to `op-geth` too (op-pp consistent) — confirm all new envs emit `op-geth`.
- Goal: Make `test/e2e/envs/loader.go` able to load an env with one *or many* L2 networks, native *or* custom gas, op-stack *or* cdk-erigon sequencer, and one *or many* aggkit services — without breaking the existing `op-pp` load.
- Context pack: `test/e2e/envs/loader.go` (the `summaryJSON` struct; the hardcoded `L2Networks["001"]`; the unconditional native-ETH `MintableERC20` deploy at lines ~293–319; `aggkitServiceName = "aggkit-001"`; `Env`/`L2Config` shapes); `test/e2e/envs/checks.go`; `test/e2e/testmain_test.go` (how `Env` is consumed); `test/e2e/bridge_utils.go` (single-`L2` assumptions); `op-pp/summary.json` schema.
- Actions:
  - Generalize `Env` to expose multiple L2 networks (e.g. `L2s []L2Config` or a map keyed by network id) while keeping a backward-compatible accessor for single-network envs (so existing `op-pp`-based code and tests keep compiling).
  - Make the `MintableERC20` auto-deploy and native-gas assumptions conditional/per-network (skip or adapt for custom-gas and cdk-erigon).
  - Parameterize the aggkit service name(s) used by `StopAggkit`/`StartAggkit`/data-dir helpers so multi-aggkit envs work.
  - Add the new `ENVName` constants (`op-fep`, `op-fep-committee`, `op-pp-2chains`, `cdk-erigon-3chains`) and any per-env capability flags the loader needs.
- Acceptance criteria: `make build` + `make lint` green in the worktree; existing `op-pp` still loads (verified by `go test -run TestMain`-style load or `envs/loader_test.go`); new constants compile; no test logic added.
- Non-goals: Creating the new snapshots (P7–P10); writing any migrated test.
- Dependencies: P2 (and P1 for the gap list); does not need P3/P4.

### P6. Generalize sanity checks and the new-stack CI for multiple envs

- Status: completed
- Execution notes (P6 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P6_LOG.md`): aggkit worktree branch `feat/e2e-envs-integration`, commit `5d137cf1` (5 files: `test/e2e/envs/checks.go`, `loader.go`, `test/e2e/testmain_test.go`, `.github/workflows/test-go-e2e.yml`, `Makefile`). **checks.go** now iterates `Env.L2s` (removed hardcoded L2 `2151908`/L1 `271828` literals → live-vs-configured per network; no MintableERC20 assertion; non-primary networks dialed via new `clientForNetwork`; primary still reuses `e.Clients.L2` so op-pp byte-identical). **CI:** `test-go-e2e.yml` now an `env_name` matrix (single `op-pp` entry + `# P11:` append markers on both `pull-docker-images` and `test-go-e2e` jobs); image list derived via `docker compose config --images | grep -v aggkit:local`; `E2E_ENV: ${{ matrix.env_name }}` plumbed; per-env artifact names; `timeout-minutes` 45→90. **Env selection:** `envs.KnownEnvs()` + `ParseENVName()`; `TestMain` reads `E2E_ENV` (default `op-pp`, unknown fails fast); post-test MintableERC20 flow guarded on `Capabilities.NativeGas`; Makefile test timeout 30m→90m. **For P11:** adding each env = pure data at the `# P11:` markers (env must have a compose file for image derivation). Live CI run infra-blocked → P11 proves green.
- Goal: Make `envs/checks.go` validate whatever topology an env declares, and turn `test-go-e2e.yml` from a single hardcoded `op-pp` run into an env-parameterized (matrix) workflow with a per-env image-pull step.
- Context pack: `test/e2e/envs/checks.go`; `.github/workflows/test-go-e2e.yml` (the `pull-docker-images` job hardcodes `cd test/e2e/envs/op-pp` and `docker compose pull geth beacon validator agglayer op-geth-001 op-node-001`, then `docker compose config --images`; the `test-go-e2e` job runs `make test-e2e` with `timeout-minutes: 45`); `Makefile` `test-e2e` target; the per-env image lists from P7–P10's compose files.
- Actions:
  - Generalize `checks.go` so health/contract sanity checks iterate over all L2 networks in the env and tolerate FEP/custom-gas/cdk-erigon differences.
  - Refactor `test-go-e2e.yml` to a matrix over env names (driven by an env-selection mechanism, e.g. an env var consumed by `make test-e2e`/`TestMain`), with `pull-docker-images` deriving the image list dynamically from the selected env's compose file (`docker compose config --images`) instead of the hardcoded list; bump `timeout-minutes` to fit heavier multi-container envs.
  - Keep a way to select which env(s) run (so an env with no migrated tests yet still just boots + sanity-checks).
- Acceptance criteria: `make build`/`make lint` green; `test-go-e2e.yml` still passes for `op-pp`; the workflow is structured to add each new env as a matrix entry with no further YAML surgery; image list is derived, not hardcoded.
- Non-goals: Adding the new envs themselves (P7–P10) or porting tests.
- Dependencies: P5

### P7. Build + integrate the `op-fep` env (FEP / op-succinct single chain)

- Status: completed
- Execution notes (P7 outcome — THUMBS_UP attempt 2, 1 change request; see `ENVS_INTEGRATION_PLAN/P7_LOG.md`): Real FEP enclave snapshotted + integrated at `test/e2e/envs/op-fep/`. Final commits: worktree `f333a89f`, kurtosis-cdk `b3e13ba9`. Env **boots healthy**, `LoadEnv`+`CheckEnv` pass, L1→L2 ERC20 bridge round-trip succeeds; `make build` RC 0, scoped lint 0. **Key fix (carry to P8):** the FEP L2 EL is **op-reth**, not op-geth — `generate-compose.sh` now emits an op-reth-correct entrypoint when the restored EL image is op-reth (kurtosis-cdk `05f04196`→`b3e13ba9`), while the summary.json logical key stays `op-geth` (loader-compat). Plus follow-on compose fixes: op-reth POST `eth_chainId` healthcheck, Teku FULU spec/genesis.ssz patch, op-succinct proposer hostname + writable `/app/configs`, and TestMain LoadEnv timeout 1m→5m (op-reth boots ~74s). **Limitation (carry to P8/P11/P12):** L2→L1 **FEP settlement does NOT complete on a restored snapshot** (op-succinct v3.5.0 enforces on-chain `rollupConfigHash`; snapshot re-anchors genesis timestamp → hash mismatch; consistent with `settled:false`) — judged architectural/out-of-scope; documented follow-up. **The live FEP enclave bring-up is FLAKY/HEAVY here** (op-succinct contract-deploy hung 50+ min on a retry) — prefer regenerating compose deterministically from captured snapshot state over re-running the enclave.
- Goal: Generate the `op-fep` snapshot from the kurtosis-cdk branch and integrate it into the aggkit worktree as a loadable, sanity-checked env.
- Context pack: kurtosis-cdk branch P4 preset for FEP (mirrors `op_succinct_args_base.json` minus `bridge_spammer` + `single_chain_op_succinct_args.json`, `consensus_contract_type: fep`, mock prover); `snapshot/snapshot.sh`; aggkit `test/e2e/envs/op-pp/` as the target layout; generalized loader/checks (P5/P6); `envs/README.md` `op-pp` entry as the metadata template.
- Actions: Spin up the FEP enclave on the branch; run `snapshot.sh`; copy the snapshot (`docker-compose.yml`, `summary.json`, `config/`, any data dir) into `test/e2e/envs/op-fep/`; add the env to the loader/CI matrix; document the generating kurtosis-cdk commit + args in `envs/README.md`.
- Acceptance criteria: `docker compose up -d` from `test/e2e/envs/op-fep/` boots healthy; `LoadEnv(ctx, "op-fep")` + `checks.go` pass; a minimal post-load FEP health/settlement probe succeeds; `make build`/`make lint` green. **No migrated test added.**
- Non-goals: Porting optimistic-mode / FEP GER / FEP bridge tests; the committee env (P8).
- Dependencies: P4, P6 (writes to both trees — serialized after the generalization steps).

### P8. Build + integrate the `op-fep-committee` env (FEP + aggoracle committee)

- Status: completed
- Execution notes (P8 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P8_LOG.md`): `op-fep-committee` env integrated at `test/e2e/envs/op-fep-committee/`. Final commits: kurtosis-cdk `d71f4265`, worktree `e7c12277`. Env boots healthy, `LoadEnv`+`CheckEnv` pass, **2/3 committee quorum probe passes** (verified independently via `cast` + loader-bound contract). **Key finding (carry to P11/P12):** the AggOracle committee is **NOT cdk-data-availability/DAC** — it's extra **aggkit services `aggkit-001-aggoracle-committee-00N`** (`--components=aggoracle`, each with its own `aggoracle-N.keystore`) + an on-chain **`AggOracleCommittee` proxy** (address in summary.json `contracts.aggoracle_committee`); loader now binds it and exposes quorum/members. **Snapshot-tool fix benefiting ALL envs:** L1-geth `--gcmode=archive` only persists on graceful SIGTERM — a force-kill left L1 at block 7 (restored op-reth couldn't reach its rollup L1-origin); the graceful-flush fix yields bootable snapshots. **Same inherited limitation:** L2→L1 FEP `settled:false` (op-succinct proposer crash-loops; out-of-scope). The live committee+FEP enclave DID come up this time (heavy/long run).
- Goal: Generate + integrate the FEP-with-aggoracle-committee env as a **separate env** (`op-fep-committee`), including the committee services/keystores in the snapshot.
- Context pack: kurtosis-cdk branch P4 committee preset (`use_agg_oracle_committee: true`, quorum 2/3, mirrors `single_chain_op_succinct_aggoracle_committee_args.json`); the committee-service capture work from P3; `MIGRATION_PLAN.md` P10 notes on the missing committee validator container/keystore; the `op-fep` env from P7 as the base; loader/checks (P5/P6). **P7 inherited facts (use these):** this env is the SAME FEP/op-succinct topology as op-fep, so reuse the op-fep env layout (incl. `config/001/op-succinct/`) and the fixed kurtosis-cdk snapshot tool at **`b3e13ba9`** (op-reth EL entrypoint, POST healthcheck, Teku FULU/genesis.ssz patch, proposer hostname + writable `/app/configs`). The L2 EL is **op-reth** (logical summary key stays `op-geth`). Expect the SAME **L2→L1 FEP-settlement limitation** (`settled:false`, architectural — out of scope) and the SAME **flaky/heavy live enclave bring-up** (op-succinct contract-deploy hung 50+ min once) — the committee+FEP enclave is even heavier; **prefer regenerating the compose deterministically from captured snapshot state** over re-running the live enclave, and cap any live bring-up wait (~15 min) before falling back. The TestMain LoadEnv timeout is already bumped to 5m.
- Actions: Stand up the committee enclave on the branch; snapshot it (ensuring committee containers + keystores are captured and restart healthy); integrate as `test/e2e/envs/op-fep-committee/`; add to loader/CI matrix; document provenance.
- Acceptance criteria: env boots with committee services healthy; `LoadEnv` + `checks.go` pass; committee membership/quorum visible via the loader's exposed contracts/clients in a minimal probe; `make build`/`make lint` green. **No migrated test added.**
- Non-goals: Porting `bridge-e2e-aggoracle-committee.bats` / committee-update tests.
- Dependencies: P7

### P9. Build + integrate the `op-pp-2chains` env (2 OP-PP L2s)

- Status: completed
- Execution notes (P9 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P9_LOG.md`): `op-pp-2chains` env at `test/e2e/envs/op-pp-2chains/`. Worktree commit `83b4abbf`; kurtosis-cdk UNCHANGED at `d71f4265` (multi-L2 capture worked out of the box). Both L2s (001=chainId 20201, 002=chainId 20202) boot healthy under one shared L1/agglayer; `LoadEnv` returns `len(Env.L2s)==2`; CheckEnv + per-chain probes pass; build/lint green. **Built via the multi-doc preset applied into ONE enclave** (sequential `kurtosis run` of the 2-doc `op-pp-2chains.yml`; chain 002 reuses L1/agglayer). **Real loader fix (carry to P10):** non-primary L2s were dialed via the aggkit node RPC (no `eth_*`) → added **`L2Config.OpGethRPCURL`** + `clientForNetwork` prefers it (single-chain envs unaffected). OP-PP is real op-geth + native ETH (no FEP/settlement caveat). NOTE: bundled `verify.sh` fails on a docker-py host bug (not a snapshot defect; bootability proven via `docker compose up`).
- Goal: Generate + integrate a 2-network OP-PP env (chains `001` + `002`) under one L1/agglayer, with the loader exposing both L2s.
- Context pack: kurtosis-cdk branch P4 2-chain preset (mirrors `op_args_chain_1.json` + `op_args_chain_2.json`; second chain reuses L1/agglayer via `deploy_l1:false`/`deploy_agglayer:false`); multi-L2 snapshot capture from P3; generalized N-network loader from P5; `op-pp/summary.json` schema (multiple `l2_networks` entries expected now).
- Actions: Bring up the 2-chain enclave; snapshot it (verify `summary.json` lists both `001` and `002` with distinct ports/contracts/accounts and both aggkit services captured); integrate as `test/e2e/envs/op-pp-2chains/`; ensure the loader exposes both L2s; add to loader/CI matrix; document provenance.
- Acceptance criteria: env boots both L2s healthy; `LoadEnv(ctx, "op-pp-2chains")` exposes ≥2 L2 networks; `checks.go` validates both; a minimal per-chain health probe passes; `make build`/`make lint` green. **No migrated test added.**
- Non-goals: Porting `bridge-e2e-2-chains.bats` (L2↔L2 bridging logic).
- Dependencies: P6 (needs N-network loader/CI; independent of P3-FEP specifics, but P3's multi-L2 capture is required — so also P3/P4). Writes to both trees → serialized.

### P10. Build + integrate the `cdk-erigon-3chains` env (3 cdk-erigon L2s, incl. custom gas)

- Status: completed
- Execution notes (P10 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P10_LOG.md`): `cdk-erigon-3chains` env at `test/e2e/envs/cdk-erigon-3chains/`. Commits: kurtosis-cdk `da0f0845`, worktree `a51f2508`. All 3 cdk-erigon L2s boot healthy (10 services); `LoadEnv` exposes 3 L2s; CheckEnv + per-chain probe pass; build/lint green. Per-chain: 001 (chainId 2151908, custom-gas, gas-token surfaced, MintableERC20 skipped), 002 (20202, custom-gas, gas-token surfaced, skipped), 003 (20203, native, deployed). **Key deviations (validated in-scope):** cdk-erigon aggkit runs `aggsender,bridge` only — **`aggoracle` component dropped** (fresh-sequencer snapshot doesn't replay post-genesis GER-manager init → aggoracle would crash-loop); chain 001 keeps default chainId 2151908; `NativeGas` reinterpreted as "native deploys permitted" with the per-network gas_token gate driving skip-vs-deploy. cdk-erigon is a real different sequencer (`cdk-erigon:v2.65.0-RC1`) — bounded snapshot-tool + loader fixes (cdk-erigon RPC selection, gas-token surfacing, per-network gas model; extends P9's per-network dialing); op-* paths untouched.
- Goal: Generate + integrate a 3-network cdk-erigon env where one chain uses a custom gas token, with the loader exposing all three L2s and handling the cdk-erigon sequencer + custom-gas specifics.
- Context pack: kurtosis-cdk branch P4 preset (mirrors `cdk_erigon_args_base.json` + `cdk_erigon_multi_chains_args_{2,3}.json` + `cdk_erigon_custom_gas_token.json`); cdk-erigon datadir capture from P3; custom-gas + cdk-erigon loader handling from P5; kurtosis-cdk `.github/tests/cdk-erigon/*` + `.github/tests/other/gas-token/*` presets. **P1 finding:** the legacy CI applies `custom_gas_token.json` to chains **001 AND 002** (003 stays native) — so this env has *two* custom-gas chains, not one; the loader/snapshot must surface the gas-token contract for both. **P9 inherited (use these):** build via the **multi-doc preset into ONE shared enclave** (sequential `kurtosis run`); the loader now dials non-primary L2s via **`L2Config.OpGethRPCURL`** + `clientForNetwork` — for cdk-erigon the RPC service name differs from op-geth (erigon sequencer/rpc), so the loader must populate each network's RPC URL from the cdk-erigon service, and `caps.NativeGas` must be FALSE for the two custom-gas chains (so the native-ETH MintableERC20 deploy is skipped) and surface the gas-token contract instead. Current HEADs to build on: kurtosis-cdk `d71f4265`, worktree `83b4abbf`. NOTE: bundled `verify.sh` has a docker-py host bug — prove bootability via `docker compose up`.
- Actions: Bring up the 3-chain cdk-erigon enclave (one custom-gas); snapshot it (verify erigon datadirs + all three `l2_networks` captured, including the gas-token contract address for the custom-gas chain); integrate as `test/e2e/envs/cdk-erigon-3chains/`; ensure the loader wires cdk-erigon RPC/contracts and the custom-gas chain correctly; add to loader/CI matrix; document provenance.
- Acceptance criteria: env boots all three L2s healthy; `LoadEnv(ctx, "cdk-erigon-3chains")` exposes 3 L2 networks with the custom-gas chain's gas-token surfaced; `checks.go` validates all three; a minimal per-chain health probe passes; `make build`/`make lint` green. **No migrated test added.**
- Non-goals: Porting `bridge-e2e-3-chains.bats` or `bridge-e2e-custom-gas.bats` logic.
- Dependencies: P9 (serialized aggkit-worktree writes; also requires P3/P4). 

### P11. Wire all new envs into the new-stack CI and prove they boot green

- Status: completed
- Execution notes (P11 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P11_LOG.md`): worktree commit `53cacc49` (3 files: `.github/workflows/test-go-e2e.yml`, `test/e2e/envs/loader.go`, `test/e2e/testmain_test.go`). build/lint green. **Trigger gating:** a `setup-matrix` job emits `env_name` dynamically; both matrices consume it via `fromJSON` — **op-pp on `pull_request`; all five on nightly `schedule` + `workflow_dispatch`** (90m timeout; per-env artifact names). **FEP gate:** new `SettlementSupported` cap (false for op-fep/op-fep-committee → their post-test L2→L1 settlement assertion is skipped, so no red on `settled:false`; true for the others + fallback). Image list derived via `docker compose config --images` (no hardcoding). **Infra-blocked / P12 follow-ups:** (i) live GH Actions run can't execute here (final verification on push); (ii) **CRITICAL: all 4 new envs reference local-only `snapshot-{geth,beacon,validator}:*` images with NO registry — CI must publish/`docker load` them before the nightly legs go green** (op-pp unaffected); (iii) actionlint not installed; (iv) a foreign port-8545 container in the occupied workspace blocked an op-pp from-scratch boot (selection proven via op-pp log + cdk-erigon probe + P7–P10).
- Goal: Add every new env as a matrix entry in `test-go-e2e.yml` so CI brings each one up, runs its (smoke-only/empty) selection, and passes within timeout — establishing the envs as CI-ready before any test is ported.
- Context pack: the P6-generalized `test-go-e2e.yml`; each env's compose image list (P7–P10); the `pull-docker-images` dynamic-image step; host-resource/timeout considerations for the heavier multi-container envs (FEP prover, 2×/3× chains, committee). **P7 finding:** op-fep boots fine from the committed snapshot via `docker compose`, but the live FEP enclave *generation* is flaky/heavy; the CI matrix leg should cover **boot/load/checks smoke only — settlement EXCLUDED** (the documented `settled:false` / L2→L1 FEP-settlement limitation). op-fep images include op-reth + op-succinct-proposer; LoadEnv uses the already-bumped 5m timeout. **P8 finding:** `op-fep-committee` likewise is BOOT/load/checks/quorum-probe smoke only (settlement excluded); it's the heaviest env (FEP + op-reth + proposer + 2× `aggkit-001-aggoracle-committee-00N` services) — budget timeout/resources. Both FEP envs' live *generation* is flaky, but their committed snapshots boot fine via `docker compose` (a general L1-geth archive graceful-flush snapshot-tool fix from P8 helps restore reliability).
- Actions: Add matrix entries for `op-fep`, `op-fep-committee`, `op-pp-2chains`, `cdk-erigon-3chains`; ensure per-env image pull/save/load works; set/raise `timeout-minutes` and any concurrency limits so heavy envs fit; optionally gate the heaviest envs behind the workflow's commented `schedule`/nightly trigger to keep PR runs fast; confirm failure artifacts/logs upload per env.
- Acceptance criteria: a CI run brings up each new env to a healthy state and passes its smoke selection within timeout; `op-pp` remains green; logs/artifacts captured per env.
- Non-goals: Porting tests into the new selections; touching the legacy `test-e2e.yml`.
- Dependencies: P7, P8, P9, P10

### P12. Documentation, provenance, and cross-repo / cross-agent handoff

- Status: completed
- Execution notes (P12 outcome — THUMBS_UP attempt 1; see `ENVS_INTEGRATION_PLAN/P12_LOG.md`): docs/provenance + handoff PREPARATION only (NO push/PR/merge/worktree-remove — those are human handoff actions). Worktree README-only docs commit `5d888bd7` (on top of `53cacc49`); kurtosis-cdk needed no docs commit (content-complete at `da0f0845`). `test/e2e/envs/README.md` documents reproducible provenance for all 4 envs (cited SHAs verified to exist; presets exist). `MIGRATION_PLAN.md` is absent from the worktree base/`origin/develop`/`feat/migrate-e2e` ref → not created; the additive "4 envs now exist" note is recorded in the coordination note for the migration agent. Branch is a clean fast-forward on `origin/develop` (no conflicts). 3 PR-ready artifacts produced. **Human handoff remaining:** (1) push both branches; (2) open the 2 PRs (descriptions in `/tmp/follow-plan/run-20260529-150038/P12/`); (3) merge order kurtosis-cdk → aggkit envs → migration rebase; (4) apply the MIGRATION_PLAN note where that file lives; (5) **publish/`docker load` the local-only `snapshot-*` images before nightly CI legs can go green**; (6) `git worktree remove` after the aggkit PR merges.
- **Post-plan verification (2026-05-30): all 5 envs pass a live block-production test.** Each env booted via `docker compose`, every chain (L1 + all L2s) asserted producing blocks (strictly increasing `eth_blockNumber` over 4–5 polls), then torn down: op-pp (L1 386→401, L2-001 46→76); op-pp-2chains (L1 281→301, L2-001 52→92, L2-002 92→132); op-fep (L1 163→178, L2-001 38→68); op-fep-committee (L1 161→176, L2-001 41→71); cdk-erigon-3chains (L1 224→244, L2-001/002/003 all advancing). **Finding + fix:** bare `docker compose up -d` could abort on a cold host when health-gated `depends_on` deps (agglayer/op-geth) are slow to become healthy; hardened `LoadEnv` to `docker compose up -d --wait --wait-timeout 600` (worktree commit **`f499b894`** on top of `5d888bd7`), verified through the real `LoadEnv`→`CheckEnv` path (op-pp boots healthy + L1→L2 bridge round-trip); `make build` + scoped lint green. **New worktree HEAD for the PR/handoff: `f499b894`.**
- Goal: Make the new envs reproducible and hand the work off cleanly — kurtosis-cdk branch ready to PR, aggkit worktree branch ready to merge into the active migration line without clobbering the other agent's work.
- Context pack: `test/e2e/envs/README.md` (per-env metadata template), `test/e2e/README.md`, `MIGRATION_PLAN.md` (its out-of-scope table now becomes unblocked), the kurtosis-cdk branch, the aggkit worktree branch + the active `feat/migrate-e2e` branch and its owner agent.
- Actions:
  - Finalize `envs/README.md` with a section per new env (kurtosis-cdk commit, the exact kurtosis args/preset, topology summary) mirroring the `op-pp` entry.
  - Update `MIGRATION_PLAN.md`'s out-of-scope note to record that the blocking envs now exist (so follow-on test-migration steps can reference `op-fep`, `op-fep-committee`, `op-pp-2chains`, `cdk-erigon-3chains`). *(Edit only via the worktree; coordinate with the active agent since that file is theirs.)*
  - Open/prepare the kurtosis-cdk branch PR (snapshot-tool + presets) and capture its commit SHA for env provenance.
  - Coordinate the aggkit worktree merge with the agent on `feat/migrate-e2e` (rebase order, who lands first), then prepare the worktree branch PR; clean up the worktree (`git worktree remove`) after merge.
- Acceptance criteria: every new env has reproducible provenance documented; kurtosis-cdk branch is PR-ready with a captured SHA; the aggkit worktree branch is rebased/merge-ready with an agreed integration order with the other agent; no migrated test was added by this plan; `MIGRATION_PLAN.md` reflects the unblocked envs.
- Non-goals: Actually porting any test; retiring legacy `test-e2e.yml` jobs.
- Dependencies: P11
